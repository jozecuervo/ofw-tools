name: Sync AI Prompts
on:
  push:
    branches: [ ai-tooling-setup ]
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sync Prompts
        run: |
          echo "Add logic to sync prompts if needed (e.g., to other repos)"


